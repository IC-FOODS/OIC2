/**
 * SettingsTreePanel.tsx
 *
 * A tree viewer used in the right drawer to display user settings.
 * Settings are modeled as a vocabulary (system vocab) for consistency.
 */

import React from 'react';

export const SettingsTreePanel: React.FC = () => {
  return (
    <div>
      <p>SettingsTreePanel placeholder – loads user/system vocab tree</p>
    </div>
  );
};
